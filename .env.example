DB_HOST=localhost
DB_PORT=5432
DB_NAME=hotfix_db
DB_USER=hotfix_user
DB_PASS=changeme

# Optional
PORT=3030

# Instructions:
# 1. Copy this file to .env and update values (do NOT commit .env).
#    cp .env.example .env    (on Powershell: Copy-Item .env.example .env)
# 2. Create the PostgreSQL user and database (see README or commands below).
# 3. Run migrations: npm run migrate:run
